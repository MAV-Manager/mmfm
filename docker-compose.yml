services:
  backend:
    image: judahpaul16/mmgcs_backend:latest
    ports:
      - "8090:8090"
    container_name: mmgcs_backend
    deploy:
      resources:
        limits:
          memory: 700m

  frontend:
    image: judahpaul16/mmgcs_frontend:latest
    ports:
      - "5173:5173"
    container_name: mmgcs_frontend
    environment:
      - CHOKIDAR_USEPOLLING=true
